# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Capacitor native projects. Anchored to the root: an unanchored `android/`
# also swallowed plugins/repulse-monitor/android/, which is hand-written
# source, so the plugin's Kotlin never reached the repository at all.
/android/
/ios/

# What Gradle leaves behind in the plugin module that is source.
plugins/*/android/build/
plugins/*/android/.gradle/

# Env
.env
.env.*
!.env.example

.env.local
.firebase/
.playwright-mcp